{
  "name": "nuxt-wundergraph-surreal",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "rome format . --write"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "npm-run-all": "^4.1.5",
    "rome": "^12.0.0",
    "turbo": "latest",
    "vitest": "^0.30.1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@7.30.0"
}
